# Employee Management System 

This project uses JavaScript classes to create an Employee Management System. There is a base class called Employee with a name and department.
There is a subclass called Manager that extends Employee and adds a teamSize property. A Company class stores employees in an array and has a method to list all employees.
